import CategoryName from "./CategoryName.js";

interface Product {
    product_id: number;
    user_id: number;
    quantity: number;
    // price: number;
    // description: string;
    // category: CategoryName;
    // imageUrl: string;
    // name: string;
    // clickCount: number;
};

export default Product;